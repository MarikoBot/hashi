/**
 * Returns if an attribute is an instance of a certain class.
 */
export declare class IsInstanceOf {
    /**
     * Verify if a value is a ButtonInteraction instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static ButtonInteraction(target: Object, key: string): void;
    /**
     * Verify if a value is a ChatInputCommandInteraction instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static ChatInputCommandInteraction(target: Object, key: string): void;
    /**
     * Verify if a value is a Collection instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static Collection(target: Object, key: string): void;
    /**
     * Verify if a value is an CommandBlockValue initial type instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static CommandBlockValueInitial(target: Object, key: string): void;
    /**
     * Verify if a value is an ContextChannel initial type instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static ContextChannelInitial(target: Object, key: string): void;
    /**
     * Verify if a value is an CoolDownManager instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static CoolDownManager(target: Object, key: string): void;
    /**
     * Verify if a value is an DataMapEntry instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static DataMapEntry(target: Object, key: string): void;
    /**
     * Verify if a value is an HashiClient instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static HashiClient(target: Object, key: string): void;
    /**
     * Verify if a value is an InterferingManager instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static InterferingManager(target: Object, key: string): void;
    /**
     * Verify if a value is an Model instance.
     * @param target The class instance.
     * @param key The attribute to set.
     * @constructor
     */
    static Model(target: Object, key: string): void;
}
